; PHP limits for Media Library (cPanel reads this from public_html).
; Host may still cap these lower in MultiPHP INI Editor / CloudLinux.
upload_max_filesize = 1024M
post_max_size = 1024M
max_execution_time = 600
max_input_time = 600
memory_limit = 512M
